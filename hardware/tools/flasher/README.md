# USB/Serial Flashing Tool

This tool provides an interface between a PC USB port and the debug port on a
SensNode device. When used in combination with 'gruf' it can be used to reset
and flash the device as well as providing access to the debug serial port on
the node.

